{
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/tracejs",
    "packages/tracejs-vue-next"
  ],
  "scripts": {
    "build": "yarn workspace tracejs build",
    "cli": "node packages/tracejs/bin/cli.js",
    "vue-dev": "yarn workspace tracejs-vue-next dev",
    "vue-build": "yarn workspace tracejs-vue-next build"
  }
}
